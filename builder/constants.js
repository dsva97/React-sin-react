import path from "path";

export const DIST_PATH = path.resolve(__dirname, "..", "dist");
export const STRING_PATH = path.resolve(DIST_PATH, "string.html");
export const STATIC_PATH = path.resolve(DIST_PATH, "static.html");
